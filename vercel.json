{
  "buildCommand": "ls -la && echo '---packages---' && ls -la packages/ && cd packages/frontend && npx vite build && cd ../admin && npx vite build",
  "outputDirectory": "packages/server/public",
  "installCommand": "npm install"
}
